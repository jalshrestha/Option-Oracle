# GitGuardian configuration
# https://docs.gitguardian.com/ggshield-docs/configuration

version: 2

ignore-paths:
  - Frontend/providers/auth-provider.tsx  # contains generated token patterns, not real secrets

ignored-matches:
  - name: "Generic Password in auth-provider.tsx — runtime-generated anonymous credential token, not a hardcoded secret"
    paths:
      - "Frontend/providers/auth-provider.tsx"
